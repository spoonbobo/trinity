---
description: Start the Trinity Docker stack
---

Start the Trinity AGI Docker Compose stack. Run:

!`docker compose -f app/docker-compose.yml up -d`

Report which services started, their status, and any errors from the output.
