# OpenClaw Gateway token (generate with: openssl rand -hex 32)
OPENCLAW_GATEWAY_TOKEN=replace-me-with-a-real-token

# Everything else (LLM providers, search keys, etc.) is configured
# through the OpenClaw dashboard at http://localhost:18789 after startup.
