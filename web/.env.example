# OpenClaw Gateway token (generate with: openssl rand -hex 32)
OPENCLAW_GATEWAY_TOKEN=replace-me-with-a-real-token

# LLM Provider API keys (uncomment the ones you use)
# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...

# Brave Search API key (for web_search tool)
# BRAVE_API_KEY=...
