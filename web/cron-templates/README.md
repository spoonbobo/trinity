This directory seeds container-local OpenClaw cron templates.

Add template files here. They are copied into `/home/node/.openclaw/cron-templates`
inside the `trinity-openclaw` container on first startup when the target folder is empty.
